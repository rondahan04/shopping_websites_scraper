# Optional: source before running gstack browse / $B so `bun` is on PATH.
#   source shopping_websites_scraper/scripts/gstack-browse-env.sh
#
# Install Node Playwright browsers once (requires network):
#   npx playwright install

export PATH="${HOME}/.bun/bin:${PATH}"
