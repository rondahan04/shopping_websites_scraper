# Core scraping (versions checked PyPI 2026-05-15)
beautifulsoup4>=4.14.3
scrapling[fetchers]>=0.4.8
httpx>=0.28.1
lxml>=6.1.0
playwright>=1.59.0
rapidfuzz>=3.14.5
tabulate>=0.10.0
python-dotenv>=1.2.2

# OpenAI SDK (GPT-5.5 and latest Chat Completions / Responses API)
openai>=2.36.0

# HTTP API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0

# Review analyzer + Visual dupe finder
transformers>=4.47.0        # CLIP model (CLIPModel, CLIPProcessor); 4.47 adds flash-attn2 support
torch>=2.6.0                # Required backend for transformers; 2.6 ships torch.compile stability fixes
Pillow>=11.1.0              # PIL image loading / resizing; 11.x drops deprecated APIs from 9.x
chromadb>=0.6.0             # Local vector DB for CLIP embeddings; 0.6 rewrites the HNSW index layer
requests>=2.32.0            # HTTP image downloads in visual_dupe_finder

# Tests
pytest>=8.0.0
